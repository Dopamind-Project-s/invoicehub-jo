@extends('layouts.guest')
@section('title', $post->meta_title ?: $post->title_ar)
@push('styles')<meta name="description" content="{{ $post->meta_description ?: $post->excerpt_ar }}"><meta property="og:title" content="{{ $post->meta_title ?: $post->title_ar }}"><meta property="og:description" content="{{ $post->meta_description ?: $post->excerpt_ar }}"><meta property="og:image" content="{{ $post->image_url }}"><link rel="stylesheet" href="{{ asset('css/blog.css') }}">@endpush
@section('content')
<section class="blog-hero"><div class="container"><span class="blog-pill mb-3">{{ $post->category }}</span><h1 class="display-6 fw-bold mb-3">{{ $post->title_ar }}</h1><div class="blog-meta">{{ $post->published_at?->format('Y-m-d') }} · {{ number_format($post->views_count) }} مشاهدة</div></div></section>
<section class="py-5"><div class="container"><img class="w-100 rounded-4 mb-5" style="max-height:480px;object-fit:cover" src="{{ $post->image_url }}" alt="{{ $post->title_ar }}"><div class="row g-5"><article class="col-lg-8"><div class="blog-content">{!! nl2br(e($post->content_ar)) !!}</div></article><aside class="col-lg-4"><div class="blog-cta sticky-top" style="top:90px"><h3 class="h4 fw-bold">جرّب InvoSync</h3><p>حوّل متطلبات الفوترة الوطنية إلى مسار عمل واضح لإصدار الفواتير وربطها مع JoFotara.</p><a class="btn btn-light rounded-pill px-4" href="{{ route('subscription-requests.create') }}">اطلب تجربة</a></div></aside></div>@if($related->isNotEmpty())<h2 class="h4 fw-bold mt-5 mb-4">مقالات مشابهة</h2><div class="row g-4">@foreach($related as $item)<div class="col-md-4"><article class="blog-card"><img class="blog-img" src="{{ $item->image_url }}" alt="{{ $item->title_ar }}"><div class="p-3"><h3 class="h6 fw-bold">{{ $item->title_ar }}</h3><a href="{{ route('blogs.show',$item->slug) }}">اقرأ المزيد</a></div></article></div>@endforeach</div>@endif</div></section>
@endsection
