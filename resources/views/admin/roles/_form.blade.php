@csrf
<div class="row g-3" dir="rtl"><div class="col-md-6"><label class="form-label">اسم الدور</label><input name="name" class="form-control" value="{{ old('name',$role->name) }}" @readonly($role->name === 'Super Admin') required></div><div class="col-md-6"><label class="form-label">Company ID اختياري</label><input name="company_id" class="form-control" value="{{ old('company_id',$role->company_id) }}" placeholder="اتركه فارغًا لدور نظام"></div><div class="col-12"><h2 class="h5">الصلاحيات</h2>@foreach($permissionGroups as $group => $names)<div class="card card-body mb-3"><h3 class="h6 fw-bold">{{ $group }}</h3><div class="row g-2">@foreach($names as $name)@if($permissions->has($name))<div class="col-md-3"><label class="form-check"><input class="form-check-input" type="checkbox" name="permissions[]" value="{{ $name }}" @checked(in_array($name, old('permissions',$assignedPermissions))) @disabled($role->name === 'Super Admin')> <span class="form-check-label">{{ $name }}</span></label></div>@endif@endforeach</div></div>@endforeach</div><div class="col-12"><button class="btn btn-primary" @disabled($role->name === 'Super Admin')>حفظ</button></div></div>
