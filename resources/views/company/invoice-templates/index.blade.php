@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h3">قوالب الفواتير</h1><span class="badge bg-info-subtle text-info">القالب الافتراضي يؤثر على PDF</span></div>
@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif
<form method="post" action="{{ route('company.invoice-templates.update', $company) }}">@csrf @method('PUT')
<div class="row g-4">@foreach($templates as $template)@php($isSelected=(string)$selected===(string)$template->id || (!$selected && $template->is_default))<div class="col-lg-4"><div class="card h-100 border-{{ $isSelected?'primary':'light' }} shadow-sm"><div class="card-body"><div class="ratio ratio-16x9 bg-light rounded mb-3 d-flex align-items-center justify-content-center overflow-hidden">@if($template->preview_image)<img src="{{ asset($template->preview_image) }}" alt="{{ $template->name }}" class="w-100 h-100 object-fit-cover" onerror="this.style.display='none';this.nextElementSibling.style.display='block'"><div style="display:none" class="p-4 text-muted">معاينة مباشرة متاحة من الزر</div>@else<span class="text-muted">لا توجد صورة معاينة</span>@endif</div><h2 class="h5">{{ $template->name }}</h2><dl class="row small text-muted"><dt class="col-5">اللغة</dt><dd class="col-7">{{ $template->language }}</dd><dt class="col-5">النوع</dt><dd class="col-7">{{ $template->layout_type }}</dd><dt class="col-5">الحالة</dt><dd class="col-7">{{ $isSelected ? 'القالب الحالي' : 'متاح' }}</dd></dl><div class="d-flex gap-2 flex-wrap"><a class="btn btn-sm btn-outline-primary" target="_blank" href="{{ route('company.invoice-templates.preview', [$company, $template]) }}">معاينة</a><button class="btn btn-sm {{ $isSelected?'btn-primary':'btn-outline-success' }}" name="invoice_template_id" value="{{ $template->id }}">اختيار كقالب افتراضي</button></div></div></div></div>@endforeach</div>
</form>
@endsection
