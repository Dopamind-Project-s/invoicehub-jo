@php($items = $integrations ?? [])
@if(empty($items)) @php($items=[['name_ar'=>'جوفوتارا','description_ar'=>'الربط مع نظام الفوترة الوطني','status'=>'available'],['name_ar'=>'واتساب','description_ar'=>'مشاركة الفواتير','status'=>'available']]) @endif
<section id="integrations" class="sp position-relative" dir="rtl"><div class="container"><div class="text-center mb-5"><span class="slbl">التكاملات</span><h2 class="stitle">تكاملات عملية تخدم <span class="gt">الفوترة اليومية</span></h2></div><div class="row g-3 justify-content-center">@foreach($items as $it)<div class="col-6 col-md-4"><div class="gc p-4 text-center h-100"><div class="ftico mx-auto">@if($it['icon_url'] ?? null)<img src="{{ $it['icon_url'] }}" alt="" style="max-width:34px;max-height:34px">@else{{ $it['icon'] ?? '✓' }}@endif</div><div class="fw-semibold mb-1">{{ $it['name_ar'] }}</div><span class="badge {{ ($it['status'] ?? '') === 'coming_soon' ? 'text-bg-secondary' : 'text-bg-info' }}">{{ ($it['status'] ?? '') === 'coming_soon' ? 'قريباً' : 'متوفر' }}</span><div style="font-size:.82rem;color:var(--tx3)">{{ $it['description_ar'] }}</div></div></div>@endforeach</div></div></section>
