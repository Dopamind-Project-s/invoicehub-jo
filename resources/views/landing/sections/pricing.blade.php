<section id="pricing" class="sp" style="background:var(--bg2)" dir="rtl"><div class="container"><div class="text-center mb-5"><span class="slbl">الباقات</span><h2 class="stitle">باقات واضحة قابلة للتوسع</h2><p class="ssub mx-auto">تُقرأ الباقات مباشرة من لوحة الإدارة مع مفاتيح المزايا المرتبطة.</p></div><div class="row g-4 align-items-stretch">@forelse($plans as $plan)<div class="col-md-4"><div class="pcard h-100 {{ $plan->is_recommended ? 'pop' : '' }}">@if($plan->is_recommended)<span class="pbadge">موصى بها</span>@endif<h3>{{ $plan->name_ar ?: $plan->name }}</h3><div class="pamt mb-1"><span>{{ number_format((float) $plan->monthly_price, 3) }}</span><sup>د.أ</sup></div><div style="font-size:.82rem;color:var(--tx3)">شهرياً / {{ number_format((float) $plan->yearly_price, 3) }} سنوياً</div><p style="font-size:.875rem;color:var(--tx2);margin:14px 0 20px;padding-bottom:20px;border-bottom:1px solid var(--bd)">{{ $plan->description_ar ?: $plan->description }}</p>@foreach($plan->featureKeys as $feature)<div class="pfl"><span class="pchk">✓</span>{{ $feature->name_ar ?: $feature->name }}</div>@endforeach<a class="bgrd btn w-100 py-2 mt-4" href="{{ route('register') }}">اختر الباقة</a></div></div>@empty<div class="col-12"><div class="gc p-4 text-center">لا توجد باقات فعالة حالياً.</div></div>@endforelse</div></div></section>
